/** @format */

import * as types from "../constants/actionTypes";

const initalState = {};

const businessReducer = (state = initalState, action) => {
  //declare variables up here

  switch (action.type) {
    // case types.   - dclare switch types here

    default:
      return state;
  }
};

export default businessReducer;
